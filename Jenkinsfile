pipeline {
	agent any

	stages {
	  stage(build) {
	    steps {
	      echo "Buildiddddng the app.."
	    }
	  }
	  stage(test) {
	    steps {
	       echo "Testinggg the app.."
	    }
	  }
  	  stage(Deploy) {
            steps {
               echo "Finallyier231Update Deploying the app.."
            }
          }
  }
}

